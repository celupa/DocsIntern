name: docs_intern
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pillow
  - requests
  - setuptools
  - sqlite
  - pandas
  - pip:
      - pymupdf
      - bitsandbytes
      - accelerate
      - transformers
      - sentence-transformers
      - torch
      - llama-cpp-python
      - scipy
      - chromadb
      - gradio